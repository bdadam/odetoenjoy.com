// import videoModel from '../src/models/video-model';
// jest.mock('../src/databases/videodb');

describe('Post video222', () => {
    beforeEach(() => {

    });

    it('bbb222', () => {
        // videoModel.create({
        //     title: 'title'
        // });
    });    
});
