export type Video = {
    title: string;
    slug: string;
    embedUrl: string;
    description: string;
    tags: string[];
    image: string;
    thumbnail: string;
    duration: string;
};

export type VideoLink = {
    title: string;
    url: string;
    thumbnail: string;
    duration: string;
};
