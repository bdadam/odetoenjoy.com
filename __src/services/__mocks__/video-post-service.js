export default {
    fetchMetaData(url) {
        console.log(url);
        return Promise.resolve({});
    },
    
    postVideo() {
        console.log('post-video');
        return Promise.resolve({});
    }
};
